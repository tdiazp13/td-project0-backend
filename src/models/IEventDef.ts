export default interface IEventDef {
    event_name: string;
    event_category: number;
    event_place: string;
    event_address: string;
    event_initial_date: Date;
    event_final_date: Date;
    event_type: number;
}
